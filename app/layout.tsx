import type { Metadata, Viewport } from "next";
import { headers } from "next/headers";
import "./globals.css";
export const viewport: Viewport = { width: "device-width", initialScale: 1, viewportFit: "cover", themeColor: [{ media: "(prefers-color-scheme: light)", color: "#f8f8f6" }, { media: "(prefers-color-scheme: dark)", color: "#090909" }] };
export async function generateMetadata():Promise<Metadata>{const h=await headers();const host=h.get("host")??"localhost:3000";const protocol=host.startsWith("localhost")?"http":"https";const origin=`${protocol}://${host}`;const image=`${origin}/og.png`;return{metadataBase:new URL(origin),title:"Nora — Un espacio seguro para hablar",description:"Acompañamiento emocional con inteligencia artificial. Un proyecto de Gerard Ramces Bollard Gonzalez por el Mes de la Prevención del Suicidio.",applicationName:"Nora",manifest:"/manifest.webmanifest",icons:{icon:"/favicon.svg"},openGraph:{title:"Nora — Un espacio seguro para hablar",description:"Una compañía digital empática para cuando necesitas hablar.",url:origin,siteName:"Nora",locale:"es_CR",type:"website",images:[image]},twitter:{card:"summary_large_image",title:"Nora — Un espacio seguro para hablar",description:"Una compañía digital empática para cuando necesitas hablar.",images:[image]}}}
export default function RootLayout({children}:Readonly<{children:React.ReactNode}>){return <html lang="es"><body>{children}</body></html>}
